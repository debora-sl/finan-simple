White rounded surface with subtle border + shadow — wraps every panel and metric.

```jsx
<Card padding="md">…</Card>
<Card interactive>Clique para ver detalhes</Card>
```

`padding`: none/sm/md/lg. `interactive` adds a hover lift. Radius is `--radius-lg` (16px); shadow `--shadow-sm`.
